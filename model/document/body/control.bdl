# standard - conventional

import hwpkit.document { RgbColor }

union Control {
  CharControl(text: string),
  TabControl(),
  LineBreakControl(),
  HyphenControl(),
  NbSpaceControl(),
  FwSpaceControl(),
  TitleMarkControl(ignore: boolean),
  MarkPenBeginControl(color: RgbColor),
  MarkPenEndControl(),
}
